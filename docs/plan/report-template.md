# 보안 온보딩 보고서 — <프로젝트명>

- 대상 경로/출처: `<경로 또는 git URL>`
- 대상 SHA256: `<파일/아카이브 대상이면 shasum -a 256 결과, 디렉터리면 주요 lockfile/아카이브 해시>`
- 검사 일시: `<YYYY-MM-DD HH:MM>`
- 검사자: secure-onboard 스킬 (`docs/draft/scan.sh`) + 사람 검토
- 최종 판정: **<🟢 1차 휴리스틱 통과 - 승인 게이트 유지 / 🟠 조건부 / 🔴 위험·중단>**

---

## 0. 실행 요약

`<무엇을 검사했고, 무엇을 발견했으며, 다음 행동이 무엇인지 한 문단으로 요약>`

## 1. 프로젝트 구조 / 구성

- 감지된 생태계: `<예: Node/npm, Docker>`
- 진입점 / 실행 경로: `<예: npm start, Makefile init, Dockerfile>`
- 파일 수, 주요 디렉토리: `<요약>`
- 라이프사이클 훅 유무: `<예: prepare → husky (저위험) / 없음 / postinstall → ??? (검토)>`

<scan.sh 의 "1. 프로젝트 구성" 섹션을 붙여넣기>

## 2. 보안 검사 결과

- 자동 스캔 요약: HIGH `<n>` · MED `<n>` · INFO `<n>` (종료코드 `<0/1/2>` — `0`=HIGH 없음 · `1`=HIGH 있음 · `2`=입력 오류/스캔 실패(결과 무효))

| 등급 | 항목 | 위치 | 사람 검토 결론 |
|------|------|------|----------------|
| 🔴 HIGH | `<설명>` | `<file:line>` | `<정탐/오탐 + 근거>` |
| 🟠 MED  | `<설명>` | `<file:line>` | `<…>` |

### 발견 상세

#### F-001 `<항목명>`

- 영향: `<설치-시 실행 / 비밀정보 유출 / 난독 페이로드 등>`
- 근거: `<파일/라인/명령/문맥>`
- 공격 경로: `<예: prepare hook → app/index.js → app/test/index.js → 외부 URL 조립 → 응답 실행>`
- 증거:

```text
<민감정보는 플레이스홀더로 치환한 재현 가능한 증거>
```

- 수동 검토(체크리스트): 의존성 신뢰도 `<…>`, git 작성자 `<…>`, 네트워크 목적지 `<…>`, 요청 맥락 `<…>`

## 3. 실행 / 설치 내역

> 사용자 승인 여부: **<승인됨 / 미승인(설치·실행 안 함)>**

| 명령 | 실행 환경 | 옵션 | 결과 |
|------|-----------|------|------|
| `<예: npm install>` | `<Docker node:22-alpine / 로컬>` | `<--ignore-scripts>` | `<성공, n개 패키지>` |
| `<예: npm test>` | `<…>` | `<…>` | `<통과/실패>` |

- 설치되지 않은/건너뛴 항목과 이유: `<…>`

## 4. 결론 및 후속 권고

- 최종 판정 근거: `<요약>`
- 권고 우선순위:
  - P0: `<즉시 중단/사용 금지/격리 필요>`
  - P1: `<설치 전 추가 확인>`
  - P2: `<후속 개선>`
- 미해결 위험: `<있다면 명시>`
