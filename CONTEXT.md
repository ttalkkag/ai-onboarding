# Secure Onboard

Secure Onboard는 Claude Code와 Codex의 로컬 CLI 작업을 실행 직전에 점검하고, 사용자가 위험을 이해한 상태에서 다음 행동을 선택하도록 돕는 선택형 실행 가드레일이다.

## Language

**지원 클라이언트**:
Secure Onboard 플러그인을 설치해 사용하는 Claude Code CLI 또는 Codex CLI.
_Avoid_: 모든 AI, 일반 터미널, Finder, 파일 탐색기

**작업 요청**:
사용자가 지원 클라이언트에 실행·설치·열기·설정·권한 변경·보안 검사를 요구한 한 번의 의도.
_Avoid_: 프로젝트 전체 판정, 영구 승인

**실행 게이트**:
지원 클라이언트가 로컬 도구를 호출하기 직전에 그 작업을 점검하는 Secure Onboard의 경계.
_Avoid_: 강제 보안 경계, 운영체제 전역 차단

**구체적 명령 요청**:
사용자가 실행할 명령 문자열을 직접 제공한 작업 요청.
_Avoid_: 의도 요청, AI가 만든 명령

**의도 요청**:
사용자가 “설치해 줘”, “열어 줘”처럼 목적만 말하고 정확한 명령 문자열은 제공하지 않은 작업 요청.
_Avoid_: 구체적 명령 요청

**사용자 요청 명령어**:
신뢰된 사용자 메시지에 사용자가 직접 제공한 명령 문자열.
_Avoid_: 대상 파일 속 명령, AI 예상 명령어

**AI 예상 명령어**:
의도 요청을 수행하기 위해 AI가 제안한 아직 실행되지 않은 명령 문자열.
_Avoid_: 사용자 요청 명령어, 실행된 명령어

**AI 실행 예정 명령어**:
AI가 로컬 도구 호출 입력으로 만든 구체적인 명령 문자열.
_Avoid_: AI 예상 명령어, 실행된 명령어

**차단된 명령어**:
실행 게이트가 실행 전에 거부한 정확한 도구 호출 명령. 차단됐으므로 실행된 명령이 아니다.
_Avoid_: 실행된 명령어, 사용자 요청 명령어

**발견 등급**:
검사에서 찾은 개별 보안 근거의 `HIGH`, `LOW`, `INFO` 중요도. 읽기 전용 검사 보고에 사용하며 그 자체가 도구 호출 상태는 아니다.
_Avoid_: 실행 판정, 프로젝트 승인

**작업 판정**:
실행·설치·열기·변경 같은 보호 작업 요청과 실행 직전 호출에 대한 `HIGH`, `LOW`, `INFO` 중 하나의 행동 등급.
_Avoid_: 안전, 위험, MED, 프로젝트 승인

**HIGH**:
보호 작업을 즉시 수행하면 중대한 보안 영향이 있거나 검사를 신뢰할 수 없어 AI 자동 실행을 차단해야 하는 작업 판정.
_Avoid_: 영구 실행 금지, 사용자 실행 차단

**LOW**:
비밀값·환경 설정·오탐 가능성처럼 사용자가 알아야 하지만 AI 자동 실행을 막지는 않는 판정.
_Avoid_: MED, 조용한 허용

**INFO**:
경고할 위험은 확인되지 않았고 로컬 활동 기록만 남기는 판정.
_Avoid_: 안전 보증, 무해함

**명령 제공**:
HIGH 차단 뒤 사용자가 실행 의사를 명시적으로 재확인했을 때 AI가 영향 설명과 함께 위험 명령을 텍스트로 보여 주는 종단 상태.
_Avoid_: 실행 허가, AI 대리 실행, 실행 확인

**명령 준비**:
재확인된 HIGH action에 대해 공용 코어가 표시 payload를 만들었지만 assistant가 실제로 사용자에게 보여 줬는지는 아직 확인하지 않은 상태.
_Avoid_: 명령 제공, 실행 확인

**응답 포함 확인**:
Stop 훅의 마지막 assistant 응답 원문에서 명령 marker와 digest를 확인한 상태. UI 전달·렌더 완료·사용자 열람을 증명하지 않는다.
_Avoid_: 실행 확인, 사용자 열람 확인

**M0 hook tracer-bullet**:
실제 Claude Code·Codex hook payload, deny·continue·result·Stop 동작만 고정하는 첫 구현 단계. 실제 보안 분석 제품 기능은 포함하지 않는다.
_Avoid_: M1 제품, 배포 준비 완료

**M1 fixture-backed alpha**:
고정 fixture로 실행 게이트의 핵심 사용자 흐름을 끝까지 검증하는 실험 단계. 일반 외부 입력에 대한 폭넓은 탐지 제품이나 배포 준비 완료 상태가 아니다.
_Avoid_: 최소 출시 제품, 일반 악성코드 탐지기

**NOT_COVERED**:
훅은 호출을 관찰했지만 현재 단계의 지원 grammar 밖이어서 Secure Onboard 판정 없이 통과시키는 coverage 상태. 보안 등급이 아니며 `INFO`나 보호 성공으로 표현하지 않는다.
_Avoid_: INFO, 허용 판정, 안전

**짧은 작업 참조(short ref)**:
HIGH 차단 뒤 사용자에게 보여 주는 session-local 식별자. 사용자가 신뢰된 인간 입력 채널에서 정해진 재확인 문구에 이 값을 직접 넣으면 공용 코어가 session·action과 single-use 재확인을 resolve한다. 모델은 ref나 내부 action을 선택하지 않는다.
_Avoid_: 모델이 선택하는 action ID, 영구 승인 토큰

**전역 활성화**:
사용자 컴퓨터의 지원 클라이언트에서 프로젝트별 비활성화를 제외한 모든 프로젝트에 실행 게이트를 적용하는 상태.
_Avoid_: 관리자 강제 설치

**프로젝트 활성화**:
사용자 영역에 한 번 설치한 공용 코어의 실행 게이트를 사용자 소유 로컬 설정에서 선택한 프로젝트에만 적용하는 상태.
_Avoid_: 프로젝트별 제품 설치, 프로젝트가 제어하는 설정, 저장소에 체크인된 활성화

**프로젝트 비활성화**:
전역 활성화보다 우선해 선택한 프로젝트에서 실행 게이트를 끄는 사용자 선택.
_Avoid_: 보안 우회 탐지, 관리자 예외 승인

**유효 보호 상태**:
현재 세션의 실제 hook 경로 확인 결과인 `VERIFIED_ACTIVE`, `OFF`, `UNKNOWN`. 사용자 scope `ON|OFF`와 별도다.
_Avoid_: scope ON만 보고 보호 중이라고 단정, 확인 불가를 OFF로 추정

**로컬 활동 기록**:
플러그인 공용 코어가 사용자 컴퓨터에 남기는 판정·차단·경고·명령 제공 이벤트 기록.
_Avoid_: AI 기억, 중앙 감사 로그, 실행 증명

**검사 캐시**:
같은 대상·작업·검사 버전의 중복 분석을 줄이기 위한 사용자 컴퓨터의 검증 기록.
_Avoid_: 프로젝트 영구 승인, 경로만으로 재사용하는 안전 판정

**참고 자료**:
탐지 아이디어를 검토하기 위해 수집한 문서·스킬·스크립트. 채택과 회귀 검증 전에는 제품 동작 계약이 아니다.
_Avoid_: 구현된 기능, 실행 가능한 제품
